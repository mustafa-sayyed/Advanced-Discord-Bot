{
  "name": "discord-bot-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "bot",
    "server"
  ],
  "scripts": {
    "start": "npm start --workspace=@adb/bot",
    "deploy": "npm run deploy --workspace=@adb/bot"
  }
}